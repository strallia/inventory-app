<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>Delete Category</title>
  </head>
  <body>
    <%- include("partials/navigation") %>
    <main>
      <h1>Confirm Deletion</h1>
      <div class="content warning">
        <div class="card">
          <form action=<%= `/categories/${category.id}/delete?_method=DELETE` %> method="POST">
            <h3 class="red">WARNING!</h3>
            <p>Deleting this category will delete all of its items:</p>
            <% if (items.length === 0) {%>
                <p><em>(No items in this category)</em></p>
            <% } else { %>
              <% items.forEach((item => {%>
                <li><%= item.item_title%></li>
              <% })) %>
            <% }; %>
            <div class="buttons-container">
              <%- include("partials/goBackButton") %>
              <button type="submit" class="submit" title="delete all"></button>
            </div>
          </form>
        </div>
      </div>
    </main>
  </body>
</html>
